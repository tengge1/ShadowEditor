# 菜单栏

由场景、编辑、二维、几何体、光源、资源、组件、播放、工具、选项、帮助几个菜单组成。

## 场景

**新建**

新建场景，当前场景会被清空。可以创建空场景和GIS场景。

**保存**

保存当前场景。如果是新场景，保存时会提示输入场景名称。如果是载入场景，则直接保存到原场景上。

**另存为**

将当前场景另存为一个新场景。

**发布场景**

将当前场景发布为静态文件，包含场景用到的所有页面、脚本、资源。方便部署在IIS、Apache、Tomcat等服务器软件上。

## 编辑

**撤销**

撤销上一步操作。

**重做**

重做撤销的操作。

**清空历史记录**

清空所有撤销、重做操作。清空侧边栏历史面板中的所有历史记录。

**复制**

将当前选中的物体复制为一个新物体。

**删除**

删除当前选中的物体。

## 二维（开发中）

为场景提供二维控件。例如：按钮、标签、面板、水平线、条形图、时间、竖直线、日期、时间圆盘、键值标签、表单、仪表、柱状图、折线图、侧边栏、散点图、饼状图、弦图、力导向图、树状图、集群图、包图、分区图等。

## 几何体

**组**

添加一个容器，可以在场景层次图将其他物体拖动到该容器上。

**平面**

添加一个平面。

**正方体**

添加一个正方体。

**圆**

添加一个圆。

**圆柱体**

添加一个圆柱体。

**球体**

添加一个球体。

**二十面体**

添加一个二十面体。

**轮胎**

添加一个轮胎。

**纽结**

添加一个纽结。

**茶壶**

添加一个茶壶。

**酒杯**

添加一个酒杯。

**精灵**

添加一个精灵，该面总是朝向照相机。

**文本**

在场景中添加文本，支持中文。

**线段**

添加一条线段。

**CatmullRom曲线**

添加一条CatmullRom曲线。

**二次贝塞尔曲线**

添加一条二次贝塞尔曲线。

**三次贝塞尔曲线**

添加一条三次贝塞尔曲线。

**椭圆曲线**

添加一条椭圆曲线。

## 光源

**环境光**

添加环境光。

**平行光**

添加平行光。

**点光源**

添加点光源。

**聚光灯**

添加聚光灯。

**半球光**

添加半球光。

**矩形光**

添加矩形光。

## 资源

**导出几何体**

导出几何体json文件。

**导出物体**

导出物体json文件。

**导出Collada文件**

导出Collada格式的模型文件。

**导出DRACO文件**

导出DRACO格式的模型文件。

**导出gltf文件**

导出gltf格式的模型文件。

**导出obj文件**

导出obj格式的模型文件。

**导出ply文件**

导出ply格式的模型文件。

**导出stl二进制文件**

导出stl格式二进制模型文件。

**导出stl文件**

导出stl格式的文本模型文件。

## 组件

**背景音乐**

添加一个背景音乐组件。

**粒子发射器**

添加一个粒子发射器组件。

**天空**

添加一个天空组件。

**火焰**

添加一个火焰组件。

**水**

添加一个水组件。

**烟**

添加一个烟组件。

**布**

添加一个布组件

**柏林地形**

添加一个柏林地形组件。

## 播放

启动播放器，播放所有音频和动画，执行所有脚本。

**播放**

在场景编辑区播放。

**全屏播放**

全屏幕播放。

**新窗口播放**

在新窗口播放。

## 工具

**纹理生成器（演示）**

使用算法生成纹理。

**整理贴图**

整理所有贴图资源。

**整理模型**

整理所有模型资源。

**整理缩略图**

整理所有缩略图资源。

**清理场景**

清理已删除的场景和所有场景历史文件。

## 选项

**渲染器**

设置渲染器属性。

**帮助器**

设置帮助器属性。

**滤镜**

设置场景滤镜效果。。

**语言**

选择不同的语言。

**调试模型**

调试模式开启，会出现未实现和测试中功能。

## 帮助

**渲染器属性**

查看渲染器相关信息。

**源码**

查看该项目源码。

**示例**

查看该项目示例。

**文档**

查看该项目文档。

**关于**

查看该项目开发人员信息。